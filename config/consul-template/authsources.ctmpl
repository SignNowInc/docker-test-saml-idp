<?php
$config = [
    'admin' => [
        'core:AdminPassword',
    ],

    'example-userpass' => [
        'exampleauth:UserPass',
        {{ range $users, $user := "SERVICE_KV_PATH/users"  | tree | explode }}
        '{{ .username }}:{{ .password }}' => [
            {{ range $k, $v := $user.parameters }}
            '{{ $k }}' => '{{ $v }}',{{ end }}
        ],{{ end }}
    ],
];
